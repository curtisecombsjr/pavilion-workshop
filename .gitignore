# Python
.venv/
venv/
**/__pycache__/
*.pyc

# LibreOffice lock files (created while rendering the deck)
.~lock.*#

# Rendered handouts / scratch (the .pptx IS tracked; the PDF is regenerated)
*.pdf

# Local Claude project instructions (machine-specific, not for the public repo)
CLAUDE.md
